# Example secrets file for Streamlit Cloud
# Copy this to secrets.toml and add your API key (DO NOT commit secrets.toml!)

# Optional: Add a default OpenAI API key for demo purposes
# Users can still override this by entering their own key in the UI
# OPENAI_API_KEY = "sk-your-key-here"
